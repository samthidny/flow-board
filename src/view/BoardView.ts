import ItemComponent from './ItemComponent';

/**
 * The View Mediator for the main drawing board that contains Items
 */
export default class BoardView {

    constructor() {
        

    }

    add(item:ItemComponent) {
        //Adds the group to the SVG canvas
    }

}